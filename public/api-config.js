// Base URL for the /api/history ticker-search backend (a Vercel serverless
// function — see /api/history.js). Leave empty ("") to use a same-origin
// relative path instead (only works when running via scripts/serve.ps1
// locally, which has its own built-in /api/history route).
window.API_BASE_URL = "";
